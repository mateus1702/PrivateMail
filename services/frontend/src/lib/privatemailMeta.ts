/** Public links for PrivateMail (not env-driven). */
export const PRIVATEMAIL_GITHUB_URL = "https://github.com/mateus1702/PrivateMail" as const;

/** Visible label next to the GitHub icon (link accessible name). */
export const PRIVATEMAIL_SOURCE_LINK_LABEL = "Source code" as const;
